@model List<Project>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/admin/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <a class="btn btn-primary mb-3" asp-area="admin" asp-controller="Project" asp-action="Create">Create</a>
        </div>
        <div class="col-lg-12">
            <table class="table">
                <thead style="border-bottom: 2px solid;">
                    <tr>
                        <th style="text-align: center;" scope="col">#</th>
                        <th style="text-align: center;" scope="col">Main Image</th>
                        <th style="text-align: center;" scope="col">Name</th>
                        <th style="text-align: center;" scope="col">ClientsName</th>
                        <th style="text-align: center;" scope="col">Description</th>
                        <th style="text-align: center;" scope="col">Filter</th>
                        <th style="text-align: center;" scope="col">Created Date</th>
                    </tr>
                </thead>
                <tbody style="height: 107px;">
                    @for (int i = 0; i < Model.Count; i++)
                    {
                    <tr>
                        <th style="text-align: center;" scope="row">@(i+1)</th>
                        <td style="text-align: center;"><img src="~/Uploads/@Model[i].MainImage" style="width: 100px" alt="Alternate Text" /></td>
                        <td style="text-align: center;">@Model[i].Name</td>
                        <td style="text-align: center;">@Model[i].ClientsName</td>
                        <td style="text-align: center;">@Html.Raw(Model[i].Description != null && Model[i].Description.Length > 20 ? Model[i].Description.Substring(0, 20) + "..." : Model[i].Description)</td>
                        <td style="text-align: center;">@Model[i].Filter</td>
                        <td style="text-align: center;">@Model[i].CreatedDate</td>
                        <td style="text-align: center;">
                            <a style="display: block;margin-bottom: 5px;" class="btn btn-success mb-1" asp-area="admin" asp-controller="Project" asp-action="Update" asp-route-id="@Model[i].Id">Update</a>
                            <a class="btn btn-warning" style="margin-bottom: 5px; padding: 6px 16px; display: block;" asp-area="admin" asp-controller="Project" asp-action="Detail" asp-route-id="@Model[i].Id">Detail</a>

                            <a style="display: block; margin-top: 5px;" class="btn btn-danger" asp-area="admin" asp-controller="Project" asp-action="Delete" asp-route-id="@Model[i].Id" onclick="return confirm('Are you sure?')">Delete</a>
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>